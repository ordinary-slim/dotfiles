[opener]
appimage = [
  { run = '%s', desc = "Run AppImage", orphan = true, for = "unix" },
]

[open]
prepend_rules = [
  { url = "*.AppImage", use = [ "appimage" ] },
  { url = "*.appimage", use = [ "appimage" ] },
]
